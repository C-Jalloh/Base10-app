import { View, Text } from "react-native";
import React from "react";

const PracticeScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        width: "100%",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text style={{ color: "#333" }}>Practice Screen</Text>
    </View>
  );
};

export default PracticeScreen;
