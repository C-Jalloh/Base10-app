import { View, Text } from "react-native";
import React from "react";

const AIScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        width: "100%",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text style={{ color: "#333" }}>AI Screen</Text>
    </View>
  );
};

export default AIScreen;
